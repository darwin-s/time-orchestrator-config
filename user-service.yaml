spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AUTH_PUBLIC_URL:http://localhost:8443}/realms/${AUTH_REALM:time-orchestrator}
          jwk-set-uri: ${AUTH_BASE_URL:http://localhost:8443}/realms/${AUTH_REALM:time-orchestrator}/protocol/openid-connect/certs